---
import author from '../data/author.json';
import book from '../data/book.json';

const year = new Date().getFullYear();

const nav = [
  { label: 'The book', href: '/the-book/' },
  { label: 'Author', href: '/author/' },
  { label: 'Press', href: '/press/' },
  { label: 'Buy', href: '/buy/' },
  { label: 'Contact', href: '/contact/' },
];
---

<footer class="surface border-t border-line">
  <div class="shell py-16">
    <div class="grid gap-12 md:grid-cols-[1.2fr_1fr_1fr]">
      <div>
        <p class="label text-fg">{book.title}</p>
        <p class="mt-3 max-w-[34ch] text-sm text-muted">
          {book.subtitle} &middot; {author.fullName}
        </p>
      </div>

      <nav aria-label="Footer">
        <p class="label text-muted">Site</p>
        <ul class="mt-2">
          {
            nav.map((item) => (
              <li>
                <a
                  href={item.href}
                  class="flex min-h-11 items-center text-sm text-muted transition-colors hover:text-accent"
                >
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div>
        <p class="label text-muted">Elsewhere</p>
        <ul class="mt-2">
          {
            author.social.map((s) => (
              <li>
                <a
                  href={s.url}
                  rel="noopener me"
                  class="flex min-h-11 items-center text-sm text-muted transition-colors hover:text-accent"
                >
                  {s.name}
                </a>
              </li>
            ))
          }
          <li>
            <a
              href={author.practiceUrl}
              rel="noopener"
              class="flex min-h-11 items-center text-sm text-muted transition-colors hover:text-accent"
            >
              Surgical practice
            </a>
          </li>
        </ul>
      </div>
    </div>

    <hr class="rule mt-14" />
    <p class="label mt-6 text-muted">
      &copy; {year} {book.title}
    </p>
  </div>
</footer>
